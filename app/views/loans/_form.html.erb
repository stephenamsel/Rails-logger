<%= form_for(@loan) do |f| %>
  <% if @loan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@loan.errors.count, "error") %> prohibited this loan from being saved:</h2>

      <ul>
      <% @loan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Amount $" %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label "Payback Date" %><br>
    <%= f.date_field :enddate %>
  </div>
  <div class="actions">
    <%= f.submit "Register loan"%>
  </div>
<% end %>
